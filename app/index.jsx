import MovieListWrapper from "../components/MovieList/MovieListWrapper";
import ScreenLayout from "../components/ScreenLayout";

export default function MainPage() {
  return (
    <ScreenLayout>
      <MovieListWrapper />
    </ScreenLayout>
  );
}
